Tasks
1) Use _cacheSpriteBuffers for web also, as 
Key Takeaway
source.start(when, offset, duration): Plays for a fixed duration, then stops. Overrides source.loop = true.

source.start(when, offset): Plays until explicitly stopped, and respects source.loop = true for continuous playback.
.
2) Add BGM stop button to example/src/App.tsx and beautify it replacing Button with TouchableOpacity